import { ChartArea, ChartValidatedDataset, ChartConfiguration } from "../models";
export declare const getConfiguration: (rootArea: ChartArea, datasets: ChartValidatedDataset[]) => ChartConfiguration;
