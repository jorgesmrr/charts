import { ChartArea, ChartValidatedDataset } from "../models";
export declare const paintLegendAndGetArea: (ctx: CanvasRenderingContext2D, x: number, y: number, width: number, datasets: ChartValidatedDataset[]) => ChartArea;
